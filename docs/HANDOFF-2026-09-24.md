# 인수인계 — 2026-09-24 세션 (브랜치 `nuri/magical-knuth-yqet7x`, PR #2)

새 세션은 이 문서부터 읽고 이어서 진행한다.

> **2026-09-25 현재: PR #2·#3·#4 모두 main에 머지·운영 배포 완료.** 아래 '작업 중'·'머지 전' 서술은 당시 기록이다.
> - #3 약관 개정 사전 공지(9/25 게시) → #2 전체 릴리스(3632d6a) → #4 비로그인 홈의 my_mail 401 제거
> - 운영 실측(9/25): 첫 화면 '만 14세 이상' 동의, `/account-deletion` 공개, 방침 10.05 시행 전문, 기존 이용자 재동의 창·저장,
>   `/admin` '운영자 확인'(PIN 없음), 충전 '결제 준비 중', 커뮤니티 글쓰기→'삭제' 표시→서버 삭제(글 3건 원복), 홈 4xx 0건
> - Supabase Redirect URLs에 `kr.nuri.mind://auth-callback` 등록 확인(허용 여부 실측)
> - 미검증: 운영자(WTA) 로그인 후 '운영자 확인' 진입, 실계정 삭제, 앱(AAB/iOS) 빌드

> **2026-09-25 재점검(브랜치 `NURI/review-work-progress-991d3e`)**
> - 확인: Vercel 운영 = `a94a23c`(READY), `npm run verify` 통과(메인 222/225KB), 마이그레이션 13개·엣지 함수 버전 문서와 일치,
>   운영 홈 콘솔 오류·4xx 0건, profiles 권한 상승 불가(UPDATE/INSERT 권한 없음), 운영자 함수 4종 is_admin 검사
> - **새로 찾아 고침**: `grant_points`를 로그인 사용자가 직접 불러 금액·키를 마음대로 보내 무한 발행 가능 →
>   라이브 마이그레이션 `grant_points_cap_2026_09`(`supabase/grant-points-cap-2026-09.sql`) 적용.
>   키 없는 지급 0, 건당·하루 10000P(이관 제외), 동시 호출 직렬화. 앱이 안 쓰는 `request_redeem` 실행권 회수.
>   포인트가 로컬 우선이라 예외 대신 조용히 깎는다(예외면 클라 아웃박스가 멈춤). 적용 전 원장 최대 행 411P·악용 흔적 없음.
>   검증: Docker Postgres 17에서 2회 적용 멱등 + 시나리오 10건, 라이브 적용 후 본문·실행권 확인
> - 원래 폴더(`NURI MIND`, main 체크아웃)는 origin보다 42커밋 뒤 + 미커밋 1,223파일(2026-09-12~13 인증 감사 작업, 기준 `cc7d344`).
>   지우지 않고 **로컬 브랜치 `backup/main-wip-2026-09-13`(97f35fe)** 에 통째로 보관(작업 트리 무변경, 푸시 안 함).
>   그 작업의 보안 지적(S1 profiles 권한상승·S2 PUBLIC 실행권·S3 grant_points·S4 redeem·S5 커뮤니티 삭제)은 라이브에서 모두 닫힘 확인.
>   남은 것: bump_like ±1 반복 호출. 정리(`git checkout -- . && git clean` 후 pull)는 운영자 확인 후에
> - 주의: 커밋 훅(`core.hooksPath=.githooks`)이 main 체크아웃에만 있는 `scripts/secret-scan.mjs`를 부른다 — worktree에선 파일이 없어 커밋 실패.
>   백업 브랜치에서 `.githooks/`·`scripts/secret-scan.mjs`를 main에 올리면 해결

> **2026-09-25 서비스 개선 2차(같은 브랜치)** — GitHub 공개 자료 조사 후 설치·연결
> | 항목 | 출처 | 상태 |
> |---|---|---|
> | 네이티브 공유 시트 `@capacitor/share` 8.0.2 + `src/lib/share.ts` | ionic-team (MIT) | 공유 7곳 교체. 앱 WebView엔 navigator.share가 없어 '복사'로만 동작하던 것 수정. 웹 메인 번들 222KB 불변(동적 import) |
> | 접근성 게이트 `@axe-core/playwright` 4.13 + `e2e/a11y.spec.ts` | Deque (MPL-2.0) | 8개 라우트 critical/serious 0. 발견·수정 2건: 글자 크기 슬라이더 이름표, 계정 삭제 페이지 메일 링크 밑줄 |
> | 상단 안전 영역(`env(safe-area-inset-top)`) | Capacitor 8 SystemBars(기본 css 모드) | 안드로이드 15+ edge-to-edge·iOS 홈 화면 앱에서 상단 바가 상태 표시줄 밑으로 들어가던 것. 웹 브라우저는 0px(실측) |
> | `.github/dependabot.yml` | GitHub 기본 기능 | npm·액션 주간 묶음 PR, 메이저 제외 |
> | `.github/workflows/security.yml` | github/codeql-action v4 · gitleaks-action v3 | 공개 저장소 + 개인 계정이라 무료 |
> | `.github/workflows/android.yml` | setup-java(Temurin 21) · android-actions/setup-android v4 | 디버그 APK 아티팩트. 서명 AAB는 업로드 키 결정 후 |
> - 이 PC 로컬 빌드 도구(사용자 승인·SDK 라이선스 수락 2026-09-25): JDK `%LOCALAPPDATA%\nuri-toolchain\jdk-21.0.12.1+1`(Adoptium, SHA-256 확인),
>   SDK `%LOCALAPPDATA%\Android\Sdk`(cmdline-tools 16111833·platform-tools·platforms/android-36·build-tools/36.0.0, SHA-1 확인). 환경변수는 영구 설정하지 않음
> - ⚠️ 프로젝트 경로에 한글('바탕 화면')이 있어 AGP가 빌드를 거부 → PowerShell에서 `subst N: <worktree>` 후 `N:\android\gradlew.bat assembleDebug`, 끝나면 `subst N: /d`
> - 검증: `npm run verify` 통과, E2E 65/65, 로컬 `assembleDebug` 성공(kr.nuri.mind · min 24 · target 36 · 플러그인 6종에 share 포함). 실기기 설치·공유 시트 실측은 미검증

> **2026-09-25 3차(같은 브랜치)**
> - 좋아요 1인 1회 — 라이브 `post_likes_2026_09`(`supabase/post-likes-2026-09.sql`): `post_likes(post_id, liker)` 해시 기록,
>   새 RPC `set_like(pid, did=기기 소유 토큰, want)`, IP당 글마다 3번·시간당 60번. 옛 클라용 `bump_like`는 'IP당 글마다 1번'으로 바뀜.
>   라이브 실측: 옛 bump ×5 → +1, 같은 토큰 재요청 불변, 같은 IP 3번째 기기 거절, post_likes anon 읽기 42501, 시험 후 숫자 원복(0)
> - 비로그인 AI 쿼터 우회 실측: XFF 두 값으로 fortune-detail 2회 → ai_usage 한 주체 n=2. 게이트웨이가 XFF를 덮어써 **우회 불가**
> - 옛 글 잠금 — 라이브 `community_legacy_lock_2026_09`: 2026-06-15 초기 글 3건 owner_hash=null(운영자만 삭제),
>   posts/comments `device_id` SELECT 회수(select('*') 불가, 명시 컬럼·insert…select('id')는 정상). 운영 /community 200 확인
> - 소개 페이지 운영 정보 머리글 en/ja 번역(값은 사업자등록 공식 표기 유지)
> - 원래 폴더(main 체크아웃) 정리 완료: `a94a23c`로 fast-forward. 작업 도구(.claude/agents·skills·hooks·rules·settings.json·nuri-audit·agent-memory-local,
>   .codex, CLAUDE.md, AGENTS.md, .githooks, scripts/secret-scan.mjs)는 남김. PR #6 머지 후 `.githooks`·`scripts/secret-scan.mjs`는 추적 파일과
>   겹치므로 그 두 개를 지우고 pull. docs/audit의 *.log 3개(ignore 대상, 백업에 없음)는 남김
> - Supabase 프로젝트 정리: 앱 = `xdcglyavndiwbbaryocx`(운영 번들 실측). PR #6의 'Supabase Preview' 실패는 `frccqswqfchwyijrewya`(이 계정 MCP로 조회 불가)의
>   GitHub 연동이 supabase/ 변경을 보고 미리보기 DB를 만들다 실패한 것. 사용자가 말한 `dqsygkzkoklsrqeulwdu`는 또 다른 프로젝트

## 이미 라이브(운영)에 반영된 것
- Supabase 마이그레이션 `audit_fixes_2026_09` — 권한 상승·음수 교환·동시 환불·좋아요 조작 차단
  (라이브가 더 최신인 `claim_all_mail`, `ai_usage/bump_ai_usage`는 건너뜀 — `supabase/audit-fixes-2026-09.sql` 주석 참고)
- Supabase 마이그레이션 `advisor_fixes_2026_09` — 로그인 전용 RPC 14개 anon 실행권 회수, 신고 숨김 남용 차단,
  RLS initplan, FK 인덱스 (`supabase/advisor-fixes-2026-09.sql`)
- 엣지 함수 배포: ai-report v9, deep-report v9, fortune-detail v8 (운영 클라와 호환 확인)

## 이 커밋 시점에 "작업 중"으로 멈춘 것 (타입·스모크·E2E 57/57·번들 221/225KB 통과 상태)
1. **결제 차단** — `src/data/features.ts`에 `PAYMENTS_ENABLED=false`, `APPLE_SIGNIN_ENABLED=false` 정의만 됨.
   아직 Charge/Premium 화면에 연결 안 됨 → 지금도 '베타 즉시 지급'(무료 지급) 상태. **머지 전 반드시 연결할 것.**
2. **커뮤니티 글 삭제 보안(아무나 남의 글 삭제 가능)** — `src/lib/community.ts`에 소유 토큰(sha256) 로직 일부 작성,
   서버 SQL `supabase/community-owner-fix-2026-09.sql`(미적용). 클라 insert/delete 경로 연결·폴백 완성 필요.
3. **앱(Capacitor) 셸** — `@capacitor/*` 의존성 설치, `src/lib/platform.ts`, `src/lib/backstack.ts`(안드로이드 뒤로가기),
   `src/lib/ads.ts` 일부 수정. `capacitor.config.ts`·android/ios 프로젝트는 아직 없음.

## 이어서 진행한 것 (2026-09-24 후속 세션)
- 1번 결제 차단 **연결 완료**: `subscribePremiumBeta`(스토어)와 Charge `onPay`가 `PAYMENTS_ENABLED=false`면 아무것도 지급하지 않음.
  충전·프리미엄 버튼은 '결제 준비 중'(비활성), 베타 안내 문구 교체. 브라우저에서 눌러도 다이아·프리미엄 기간 불변 확인.
- 2번 커뮤니티 클라는 글 경로(insert·select·delete) + 폴백 완성 상태 확인. 댓글 삭제 UI는 원래 없음.
  서버 SQL은 운영자 승인 후 **라이브 적용 완료**(마이그레이션 `community_owner_fix_2026_09`, 2026-09-24).
  확인: 원문 device_id 남은 행 0, 삭제 RPC 2종 owner_hash 비교, 트리거 2종, 공개값(device_id)으로 지울 수 있는 글 0/3.
- `scripts/smoke.mjs`: Windows(CRLF) 체크아웃에서 PUBLIC_ROUTES 파싱이 실패하던 것 수정.
- 검증: smoke·build·bundle 통과, E2E 57/57(로컬은 `.env` 복사 + `VITE_SUPABASE_URL` 환경변수 필요 — auth-switch 테스트가 세션 키를 그 값으로 만든다).

- 이후 같은 날 '전체 진행'으로 아래를 모두 마침(커밋 `517d72a`~`9cca472`, 전부 PR #2 브랜치에 푸시, main 머지 안 함):

| 항목 | 결과 | 운영 반영 |
|---|---|---|
| 앱 셸 | `capacitor.config.ts`, `android/`·`ios/` 생성, `src/lib/native.ts`(뒤로가기·딥링크·상태바), `npm run cap:sync` | 빌드 미검증(이 PC에 Java·SDK·맥 없음) |
| 앱 로그인 | 외부 브라우저 + `kr.nuri.mind://auth-callback`, **앱만 PKCE**, 앱이 시작한 로그인만 수락(로그인 CSRF 차단) | Supabase Redirect URLs 등록 필요 |
| 계정 삭제 | 프로필 버튼, `economy.deleteAccount`, 공개 페이지 `/account-deletion`, 엣지 함수 `delete-account` | **v2 배포**, anon 401 확인, 실계정 삭제 미검증 |
| Apple 로그인 | `AppleLoginButton`(온보딩·우편함·프로필), `APPLE_SIGNIN_ENABLED=false` | Apple provider 설정 후 켬 |
| UGC | 차단한 사람 댓글도 가림, 차단 목록·해제 UI | — |
| 아이콘 | `scripts/make-icons.mjs` → 192/512/1024, 네이티브 아이콘·스플래시 | — |
| 문서 | `docs/STORE.md`(등록·등급·개인정보 답변서·심사 노트), `docs/BILLING.md`(결제 켜기 전 조건) | — |
| 약관 | 개인정보처리방침 정비(항목·기기 저장·수탁자·국외 이전·계정 삭제), 시행 2026-10-05, `LEGAL_VERSION` 올려 재동의, 가입 동의에 '만 14세 이상' | 법률 검토 전 초안, 시행 7일 전 공지 필요 |
| 버그 스윕 | 39라우트×3언어×2폭×2테마 → 가로 넘침 2건 수정, 이후 0건 | — |
| 번들 검사 | `bundle-check`가 동적 청크를 메인으로 재던 문제(0KB 거짓 통과) 수정 — 실제 221/225KB | — |
| 카카오 심사 | `docs/kakao-review/`(운영 사이트 이용 동선 캡처 + README), 바탕 화면 `카카오심사_누리마인드` 폴더에도 복사 | 운영자가 제출 |
| 독립 보안 검토 | 높음 2(앱 OAuth)·중간 1(운영자 로컬 다이아 버튼 → 개발 빌드만)·낮음 3 수정 | — |

## 남은 할 일 (우선순위)
1. ✅ PR #2 머지·배포 완료(2026-09-25). 남은 확인: 운영자(WTA) 카카오 로그인 → '운영자 확인'으로 콘솔 진입되는지(운영자 본인만 가능)
2. 보안 검토에서 남긴 낮음 항목:
   - 옛 방식 글 3건(비밀=deviceId, 약 31비트)은 오프라인 추측 가능 — 옛 클라(main)가 사라지면 SQL 파일 '(선택)' 블록으로 운영자만 삭제 가능하게,
     이후 posts/comments의 owner_hash·device_id SELECT 권한 회수(옛 클라가 select('*')라 서비스워커 캐시가 새 버전으로 바뀐 뒤 — 며칠 여유)
   - 다이아·프리미엄은 로컬 상태(localStorage)라 `PAYMENTS_ENABLED`는 화면 가드일 뿐 — 결제 도입 시 서버 원장이 전제(docs/BILLING.md)
   - (추정) 비로그인 AI 쿼터가 `x-forwarded-for` 첫 값 기준 → 우회 가능성 실측 필요
3. 앱 빌드: Android Studio로 AAB 서명 빌드, iOS는 맥/클라우드 빌드 — 실기기에서 로그인 딥링크·뒤로가기 확인
4. 스토어 결제(Play Billing/StoreKit)와 서버 영수증 검증 — docs/BILLING.md 조건 충족 후
5. en/ja 화면의 한국어 잔존: 띠 운세 페이지(한국어 검색용, 설계대로)·약관(한국어 원문)·사업자 정보는 의도. 소개 페이지 표 머리글('상호' 등)만 번역 여지

## 운영자(사용자)가 직접 할 일
- Supabase → Edge Functions → Secrets: `ANTHROPIC_API_KEY` 또는 `GEMINI_API_KEY` (없어서 AI 기능 3개가 정적 폴백만 동작 중)
- ✅ Supabase Auth → Redirect URLs: 웹 `https://www.nurimind.co.kr/**` 허용 + 앱용 `kr.nuri.mind://auth-callback` 등록(9/25, 실측 확인)
- 카카오 디벨로퍼스 비즈 심사 재제출: 바탕 화면 `카카오심사_누리마인드` 폴더의 `00`·`01` 이미지 + README 설명 문구
- ✅ 약관 개정 사전 공지 게시(2026-09-25, PR #3 → main 배포 확인) — 시행 2026-10-05. PR #2 머지로 개정 전문(10.05 시행)이 게시되고 공지 카드는 대체됨
- Supabase Auth: Leaked password protection 켜기
- 문의 이메일·대표 전화·통신판매업 신고번호 전달
- Claude 권한 확인 창을 없애려면 세션 설정에서 권한 모드를 바꾸거나 "항상 허용" 선택

## 확인된 운영 결정
- 운영자 계정 'WTA'(2026-06-18) — 본인 계정 확인됨
- EQ 검사는 만들지 않음, 즐겨찾기는 기존 검사로
- 저작권 표기가 필요한 외부 에셋은 쓰지 않음(직접 제작). Fluent Emoji(MIT)·나눔스퀘어라운드(OFL)는 라이선스 파일만 동봉, 화면 표기 없음
- 설문은 '준비 중'(`SURVEYS_ENABLED=false`)
- 애드센스 자동 광고 꺼져 있음(운영자 확인)
